{
  "name": "zapline-crm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mini CRM SaaS de vendas e relacionamento pelo WhatsApp.",
  "scripts": {
    "dev": "wrangler dev",
    "dev:static": "npx serve public",
    "test": "node --test worker/test/worker.test.mjs test/firestore-indexes.test.mjs",
    "deploy": "wrangler deploy",
    "deploy:rules": "firebase deploy --only firestore:rules,firestore:indexes"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "license": "UNLICENSED"
}
