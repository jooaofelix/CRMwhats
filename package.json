{
  "name": "zapline-crm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mini CRM SaaS de vendas e relacionamento pelo WhatsApp.",
  "scripts": {
    "dev": "npx serve public",
    "test": "node --test worker/test/worker.test.mjs",
    "deploy:pages": "wrangler pages deploy public --project-name zapline-crm",
    "deploy:worker": "cd worker && wrangler deploy",
    "deploy:rules": "firebase deploy --only firestore:rules,firestore:indexes"
  },
  "license": "UNLICENSED"
}
